{% extends "layout.html.twig" %}

{% block title %}
    Inscription
{% endblock %}

{% block content %}
    <div class="row ">
        <div class="col-md-3 col-offset-6  my-5 centered designform inscriptionformMembre">
            {% for message in app.flashes('notice') %}
                <div class="alert alert-success">
                    {{ message }}
                </div>
            {% endfor %}
            <h3>Je m'inscris</h3>
            {{ form( form, { attr: { novalidate: 'novalidate' } } ) }}
        </div>
    </div>
{% endblock %}